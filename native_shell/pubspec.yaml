name: oneday_rhythm
description: "一天话题节奏 · 原生壳（WebView 加载公网 PWA，服务器更新即同步）"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
